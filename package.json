{
  "name": "docker-ci",
  "version": "1.0.0",
  "description": "Docker CI Labs (with nodejs)",
  "main": "app.js",
  "scripts": {
    "dev": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "maximum.zero95@gmail.com",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1"
  }
}
